# SPRINT 1B.7 FINAL CERTIFICATION

## Objective
Final verification of the Frontend ↔ Backend contract alignment before attempting Sprint 1C execution.

## Verification Checklist

1. **Can Create Strategy work?**
   ❌ NO. Fails with a `422 ValidationError` because the frontend omits required dummy blocks (`buy_logic`, `sell_logic`, `risk`) expected by `StrategyBlueprint`.

2. **Can Save Strategy work?**
   ❌ NO. See Create Strategy.

3. **Can Validate DAG work?**
   ❌ NO. The frontend implements client-side validation and skips invoking the `/api/strategies/validate` endpoint entirely.

4. **Can Backtest work?**
   ❌ NO. The backend engine explicitly looks for the DAG within the root `dag` object, but the frontend serializes it deeply into `params.dag_nodes`. The payload is ignored and falls back to a crashing legacy state.

5. **Can Deploy work?**
   ❌ NO. The `deploy_bot` API expects a `DeployRequest` schema containing `{ exchange_id }`, which the frontend never provides. Furthermore, the Builder UI attempts to hit a phantom `POST /deploy` endpoint instead of resolving the strategy ID.

6. **What exact blockers remain?**
   * Pydantic schema validation failures on Strategy Save.
   * Deeply nested DAG arrays failing Backtest schema parsing.
   * Deploy payload lacking exchange context.
   * `serializeReactFlowToDAG()` is utterly unbuilt; raw UI nodes are passed directly to the backend without parsing.

7. **Is Sprint 1C allowed to begin?**
   ❌ NO. Sprint 1C cannot begin because the DAG configuration generated by the React frontend cannot legally traverse the API boundary into the FastAPI engine.

## Verdict
**STATUS: NOT READY**

An absolute prerequisite to Sprint 1C is applying the `FRONTEND_API_PATCHSET.md` and writing the missing `serializeReactFlowToDAG()` function so that nodes match the `DAGNode` schema originally certified during Sprint 1A.
