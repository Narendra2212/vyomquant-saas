# Deployment Package Report

> [!NOTE]
> Copies have been successfully created in `backend_app/` and `connection_layer/`.

## backend_app/ Package

**266 files** copied into `backend_app/`

## connection_layer/ Package

**227 files** copied into `connection_layer/`

