# ═══════════════════════════════════════════════════════════════════════
#  Aerora Quant Backend — Python dependencies
#  Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════════════

# ── FastAPI + ASGI server ───────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==12.0
python-multipart==0.0.9

# ── Auth + Database ─────────────────────────────────────────────────────
supabase==2.7.4
cryptography==43.0.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── Redis Cache (FIX N9: was missing from original) ────────────────────
redis==5.0.8

# ── Payment Gateways (FIX N9: both were missing from original) ─────────
stripe==10.8.0
razorpay==1.4.1

# ── Exchange Connectivity ───────────────────────────────────────────────
ccxt[async]==4.3.92
aiohttp==3.10.5
toolz==0.12.1  # Required by ccxt, pin version to avoid TypeError

# ── DEV MODE NOTE ───────────────────────────────────────────────────────
# To run in DEV_MODE without Supabase/QuestDB:
#   set DEV_MODE=true (or ENV=development)
#   pip install fastapi uvicorn websockets python-multipart redis pydantic httpx

# ── Numba + Numeric stack ───────────────────────────────────────────────
numba==0.60.0
numpy==1.26.4
pandas==2.2.2

# ── Backtesting ─────────────────────────────────────────────────────────
vectorbt==0.26.2
plotly==5.22.0      # vectorbt dependency
ta==0.11.0          # vectorbt dependency

# ── ML / Tree Models ────────────────────────────────────────────────────
xgboost==2.1.1
lightgbm==4.3.0
catboost==1.2.3
scikit-learn==1.5.1
joblib==1.4.2

# ── Deep Learning (FIX N9: tensorflow was missing) ─────────────────────
tensorflow==2.16.1   # For LSTM, GRU, Transformer, Autoencoder blocks

# ── Utilities ───────────────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic[email]==2.8.2
pydantic-settings==2.4.0
httpx==0.27.0
wsproto==1.2.0
sentry-sdk[fastapi]>=2.0.0

# ── Observability ───────────────────────────────────────────────────────
prometheus-client>=0.17.0
asgi-correlation-id>=4.3.0
